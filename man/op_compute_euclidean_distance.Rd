% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/op_compute_euclidean_distance.R
\name{op_compute_euclidean_distance}
\alias{op_compute_euclidean_distance}
\title{Compute Euclidean Distance}
\usage{
op_compute_euclidean_distance(data, overwrite = FALSE)
}
\arguments{
\item{data}{A data frame containing the columns to process.}

\item{overwrite}{Logical value indicating whether to remove original 'x' and 'y' columns.}
}
\value{
A data frame with Euclidean distance columns added.
}
\description{
This function calculates the Euclidean distance for each pair of columns that include
'x' and 'y' (regardless of prefixes) and creates new columns with appropriate labels.
}
\examples{
# Create a sample dataframe
data <- data.frame(
  a_x = c(1, 2, 3),
  a_y = c(4, 5, 6),
  b_x = c(7, 8, 9),
  b_y = c(10, 11, 12)
)

# Compute Euclidean distances without overwriting original columns
cleaned_data <- op_compute_euclidean_distance(data, overwrite = FALSE)
print(cleaned_data)

# Compute Euclidean distances and overwrite original columns
cleaned_data_overwrite <- op_compute_euclidean_distance(data, overwrite = TRUE)
print(cleaned_data_overwrite)
}
